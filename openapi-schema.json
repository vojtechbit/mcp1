{
  "openapi": "3.1.0",
  "info": {
    "title": "MCP Assistant API",
    "version": "3.1.0",
    "description": "OAuth proxy API used by a Custom GPT to access Gmail, Calendar, Contacts (Google Sheets), and Google Tasks.\n\nDesign principles (for the model):\n- The schema is the source of truth. Follow the per-endpoint descriptions and response semantics.\n- Use Europe/Prague as the reference timezone. If a `relative` parameter exists, send it instead of converting times yourself.\n- For read-heavy overviews, prefer search/list with lightweight previews (e.g., `include=summary`) and use aggregate pagination when you truly need completeness.\n- When a response indicates `hasMore` or `partial`, either continue paging/aggregate or explicitly tell the user you're showing a subset.\n- Never invent content. Only render data that actually comes from a tool response.\n\nSecurity:\n- Authorization uses OAuth 2.0 Authorization Code (handled by this server)."
  },
  "servers": [
    {
      "url": "https://mcp1-oauth-server.onrender.com",
      "description": "Primary API server"
    }
  ],
  "security": [
    {
      "oauth2": []
    }
  ],
  "components": {
    "securitySchemes": {
      "oauth2": {
        "type": "oauth2",
        "flows": {
          "authorizationCode": {
            "authorizationUrl": "https://mcp1-oauth-server.onrender.com/oauth/authorize",
            "tokenUrl": "https://mcp1-oauth-server.onrender.com/oauth/token",
            "scopes": {
              "gmail": "Access Gmail features",
              "calendar": "Access Calendar features",
              "contacts": "Access Contacts features",
              "tasks": "Access Tasks features"
            }
          }
        }
      }
    },
    "schemas": {
      "AuthStatus": {
        "type": "object",
        "properties": {
          "authenticated": {
            "type": "boolean"
          },
          "email": {
            "type": "string"
          }
        }
      },
      "Error": {
        "type": "object",
        "properties": {
          "error": {
            "type": "string"
          },
          "message": {
            "type": "string"
          }
        }
      }
    }
  },
  "paths": {
    "/api/auth/status": {
      "get": {
        "operationId": "getAuthStatus",
        "summary": "Check authentication status",
        "description": "Call this before accessing personal data. If the user is not authenticated, ask them to log in.",
        "responses": {
          "200": {
            "description": "Auth status",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/AuthStatus"
                }
              }
            }
          },
          "401": {
            "description": "Not authenticated",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Error"
                }
              }
            }
          }
        }
      }
    },
    "/api/gmail/search": {
      "get": {
        "operationId": "searchEmails",
        "summary": "Search emails",
        "parameters": [
          {
            "name": "query",
            "in": "query",
            "required": false,
            "schema": {
              "type": "string"
            },
            "description": "Gmail search syntax"
          },
          {
            "name": "maxResults",
            "in": "query",
            "required": false,
            "schema": {
              "type": "integer",
              "minimum": 1,
              "maximum": 100
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Search results"
          }
        }
      }
    }
  }
}
